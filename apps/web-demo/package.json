{
  "name": "@zupulse/web-demo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "rspack serve --mode development",
    "build": "rspack build && node ./scripts/verify-assets.mjs",
    "test:e2e": "playwright test -c playwright.config.ts",
    "test:recognition:e2e": "playwright test -c e2e/recognition.config.ts",
    "test:telemetry:e2e": "playwright test -c e2e/telemetry.config.ts"
  },
  "dependencies": {
    "@zupulse/app-i18n": "workspace:*",
    "@zupulse/web-core": "workspace:*",
    "@zupulse/web-storage": "workspace:*",
    "@zupulse/web-viewer": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1",
    "fake-indexeddb": "^6.2.5"
  }
}
