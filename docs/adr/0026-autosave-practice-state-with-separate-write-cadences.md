# 练习状态按数据性质自动保存

Practice Sidecar 在循环、速度或轨道设置变化稳定 500 ms 后防抖保存；Local Playback Resume 在播放期间最多每 5 秒保存一次，并在暂停、停止、换谱和关闭窗口时立即保存。换谱前必须等待两类写入完成，同一 Score Identity 的写入严格串行，旧状态不得覆盖新状态。应用不提供手动保存或未保存标记；崩溃最多允许丢失约 5 秒播放位置，但不应丢失已稳定超过防抖窗口的练习设置。
