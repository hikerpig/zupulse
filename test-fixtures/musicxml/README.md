# MusicXML fixtures

`generated/` 中的文件由 `pnpm fixtures:musicxml` 确定性生成，仅包含本项目原创的最小测试乐谱（MIT，与仓库一致）。不得把来源或授权不明的用户乐谱提交到此目录。

覆盖单声部、双 staff、多 part、大型总谱、timewise、repeat、歌词、中文元数据、空谱，以及损坏的 XML/MXL。`simple.mxl` 是标准单 rootfile 容器。
